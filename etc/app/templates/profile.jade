header
    .text-center: strong Profile

.profile
    .text-center
        .avatar
            img(width=124 height=124 src=model.photo_url)
        h3.user-title= model.display_name
        .wallet
            | Wallet: #{model.points}
    .row(style="margin-top: 2rem; margin-bottom: 2rem")
        .col-xs-4.text-center
            .circled #{model.participated}
            small Participated
        .col-xs-4.text-center
            .circled #{model.created}
            small Created
        .col-xs-4.text-center
            .circled #{model.friends}
            small Friends

.history
    .item.card
        .pull-left
            .category.mystery
        .cnt
            strong.bigger-text.shine-text Today

        .clearfix

    .item.card
        .pull-left
            .category.geo
        .cnt
            strong.bigger-text.shine-text Today

        .clearfix

    .item.card
        .pull-left
            .category.selfie
        .cnt
            strong.bigger-text.shine-text Today

        .clearfix